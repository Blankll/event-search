# SQLKit Enterprise Database Stack
#
# Databases requiring license acceptance or registry login.
# H2 is freely available; Oracle & DB2 need manual steps.
#
# Start:  docker compose -f docker-compose-sqlkit-enterprise.yml up -d
# Stop:   docker compose -f docker-compose-sqlkit-enterprise.yml down
# Reset:  docker compose -f docker-compose-sqlkit-enterprise.yml down -v

name: sqlkit-enterprise

services:
  # ── H2 (Java embedded DB — server mode) ────────────────────────
  h2:
    image: oscarfonts/h2:latest
    container_name: sqlkit-h2
    ports:
      - '18082:8082'   # H2 Console (web UI)
      - '19092:9092'   # H2 TCP server
    environment:
      H2_OPTIONS: -tcp -tcpAllowOthers -web -webAllowOthers
    networks:
      - sqlkit-enterprise-net
    # Default user: sa / pass: (blank)
    # JDBC URL: jdbc:h2:tcp://localhost:19092/mem:test
    # Console:  http://localhost:18082

  # ── Oracle Database Express Edition ─────────────────────────────
  # oracle:
  #   image: container-registry.oracle.com/database/express:21c
  #   container_name: sqlkit-oracle
  #   environment:
  #     ORACLE_PWD: ${ORACLE_PASSWORD:-Oracle#123}
  #     ORACLE_CHARACTERSET: AL32UTF8
  #   ports:
  #     - '1521:1521'
  #   volumes:
  #     - oracle_data:/opt/oracle/oradata
  #   networks:
  #     - sqlkit-enterprise-net
  #   shm_size: 2g
  #   # Manual setup required:
  #   #   1. docker login container-registry.oracle.com
  #   #   2. Accept license at https://container-registry.oracle.com
  #   #   3. docker pull container-registry.oracle.com/database/express:21c
  #   # Default SID: XE, user: system / pass: Oracle#123
  #   # Start takes 2-5 minutes.

  # ── IBM Db2 Community Edition ──────────────────────────────────
  # db2:
  #   image: icr.io/db2_community:latest
  #   container_name: sqlkit-db2
  #   privileged: true
  #   environment:
  #     DB2INSTANCE: db2inst1
  #     DB2INST1_PASSWORD: ${DB2_PASSWORD:-Db2#pass123}
  #     LICENSE: accept
  #     DBNAME: sqlkit
  #     BLU: false
  #     ENABLE_ORACLE_COMPATIBILITY: false
  #   ports:
  #     - '50000:50000'
  #   volumes:
  #     - db2_data:/database
  #   networks:
  #     - sqlkit-enterprise-net
  #   # Manual setup required:
  #   #   1. docker login icr.io (IBM Container Registry)
  #   #   2. docker pull icr.io/db2_community:latest
  #   # Default user: db2inst1 / pass: Db2#pass123
  #   # Start takes 3-5 minutes.

volumes:
  oracle_data:
  db2_data:

networks:
  sqlkit-enterprise-net:
    driver: bridge
